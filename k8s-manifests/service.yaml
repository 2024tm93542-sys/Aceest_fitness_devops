apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: aceest-fitness
spec:
  type: LoadBalancer
  selector:
    app: flask-app
    version: blue # On the very first run, it looks for Blue. The pipeline patches this dynamically afterwards.
  ports:
    - port: 80
      targetPort: 5000